{
  "name": "incremental-lexer-core",
  "version": "0.1.0",
  "description": "Configurable-rule incremental lexer core (TypeScript)",
  "main": "dist/src/index.js",
  "types": "dist/src/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "tsc && node --test dist/test/"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "typescript": "^5.5.0"
  }
}
